apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: yvoke

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - pvc.yaml

generators:
  - ksops-secret-generator.yaml
