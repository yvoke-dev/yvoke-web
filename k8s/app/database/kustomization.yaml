apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: yvoke

resources:
  - cluster.yaml
  - configmap.yaml
  - scheduled-backup.yaml

generators:
  - ksops-secret-generator.yaml
